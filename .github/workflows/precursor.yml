name: precursor

on:
	pull_request:
		branches:
			- main
		types:
			- closed

jobs:
	check-bats-version:
		runs-on: ubuntu-latest

		steps:
			- uses: actions/checkout@v3
			- run: echo 'hello precursor'
