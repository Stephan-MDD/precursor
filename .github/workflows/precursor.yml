on:
    push:
        branches:
            - main

jobs:
    precursor:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3
            - name: precursor
              run: git branch --show-current
